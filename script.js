/* global $ */

$(document).ready(function(){
    
    $(".quest").hide();
    
    // $("")
    
    // var current=0;

    // function nextQuestion() {
    // var next = current + 1;
    // var currentSel = '.quest' + current;
    // var nextSel ='.quest' + next;
   
    // $(currentSel).hide();
    // $(nextSel).show();
   
    // current+= 1;
    // }
    // $('.selection').click(function() {
    //     nextQuestion();
    // }); 
    
    function nextQuestion(){
        // so what i need to do is make it go to the next page when the button is clicked
        var next = 
    }
});